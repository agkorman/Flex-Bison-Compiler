// backend implementation required: a service may only target services in its
// own network, or in a network linked at the app level
app MyApp {
    network edge {
        proxy web using "nginx:latest";
        web -> backend;
    }
    network core {
        service backend using "node:20";
    }
}
