app FullStack {
    network main {
        proxy web using "nginx:latest";
        service backend using "node:20";
        database db using "postgres:16";
        cache store using "redis:7";
        expose web on 8080;
        expose backend on 3000;
        web -> backend;
        backend -> db;
        backend -> store;
    }
}
