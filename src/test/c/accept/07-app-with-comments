// A valid app that uses line comments throughout
app CommentedApp {
    // The only network of this app
    network main {
        // A reverse-proxy service
        proxy ui using "nginx:stable";
        // A generic application service
        service server using "node:20";
        // Expose the proxy publicly
        expose ui on 80;
    }
}
