// A service may target a service in another network when an app-level
// link between both networks exists
app CrossNet {
    network edge {
        proxy web using "nginx:latest";
        web -> backend;
    }
    network core {
        service backend using "node:20";
    }
    edge -> core;
}
