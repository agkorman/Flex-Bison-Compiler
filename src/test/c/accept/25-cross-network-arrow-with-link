// cross-network arrows are valid when the networks are linked
app CrossNet {
    network edge {
        proxy web using "nginx:latest";
        web -> backend;
    }
    network core {
        service backend using "node:20";
    }
    edge -> core;
}
