// backend implementation required: an out-of-range port literal must not
// wrap around into the valid 1-65535 range (4294967297 mod 2^32 is 1)
app MyApp {
    network main {
        proxy web using "nginx:latest";
        expose web on 4294967297;
    }
}
