// duplicate mount of the same source on the same service and path
app MyApp {
    network main {
        database db using "postgres:16";
        volume pgdata;
        mount pgdata on db at "/var/lib/postgresql/data";
        mount pgdata on db at "/var/lib/postgresql/data";
    }
}
