// backend implementation required: exposing the same service on the same
// port twice would emit duplicate "ports:" entries in the Compose file
app MyApp {
    network main {
        proxy web using "nginx:latest";
        expose web on 8080;
        expose web on 8080;
    }
}
