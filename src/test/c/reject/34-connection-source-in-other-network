// connections must be declared in the network of their source
app MyApp {
    network edge {
        proxy web using "nginx:latest";
    }
    network core {
        service backend using "node:20";
        web -> backend;
    }
    edge -> core;
}
