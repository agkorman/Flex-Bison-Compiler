// backend implementation required: a connection must be declared in the
// network of its source service, otherwise the generator would drop it
app MyApp {
    network edge {
        proxy web using "nginx:latest";
    }
    network core {
        service backend using "node:20";
        web -> backend;
    }
    edge -> core;
}
