// volumes are scoped to their network
app MyApp {
    network data {
        database db using "postgres:16";
        volume pgdata;
    }
    network apps {
        service backend using "node:20";
        mount pgdata on backend at "/data";
    }
}
