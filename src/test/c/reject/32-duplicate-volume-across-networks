// backend implementation required: volume names are global because they
// become Docker Compose volume keys, so they cannot repeat across networks
app MyApp {
    network data {
        database db using "postgres:16";
        volume shared;
    }
    network apps {
        service backend using "node:20";
        volume shared;
    }
}
